import { SignUpLayout } from "@/features/layout/SignUpLayout/SignUpLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <SignUpLayout>{children}</SignUpLayout>;
}
