import { SignUpLayout } from "@/features/layout/SignUpLayout/SignUpLayout";

const SignupGroupLayout = ({ children }: { children: React.ReactNode }) => (
  <SignUpLayout>{children}</SignUpLayout>
);

export default SignupGroupLayout;
